import styles from './TargetView.module.css';

export default function TargetView() {
  return (
    <div class={styles.root}>
      <div class={styles.placeholder}>
        <p class={styles.placeholderText}>AprilTag target display coming soon</p>
      </div>
    </div>
  );
}